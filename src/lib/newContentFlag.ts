// sessionStorage keys used to open a freshly created entry straight into its
// editor on first visit, without relying on a URL query param (which would
// force these detail routes out of static rendering).
export const NEW_WRITEUP_FLAG_KEY = 'shellnotes-new-writeup';
export const NEW_CHEATSHEET_FLAG_KEY = 'shellnotes-new-cheatsheet';
